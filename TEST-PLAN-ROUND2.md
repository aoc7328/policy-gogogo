# 第二輪實測劇本（貼給新的 Claude Code 對話串）

> **給 Vincent**：整份貼進新對話串就能跑。
> 核心目標不是抓 bug，是回答一個問題——
> **「把這套系統交給一個沒碰過的人當助理，他自己一個人辦得起來嗎？」**

---

## 你的任務

對 `F:\policy-gogogo`（保險知識星攻略）做一次**可用性實測**，實際跑完兩場遊戲。
重點在**流程順不順、新手助理會不會卡住**，不是找程式 bug。

**不要修改任何程式碼。** 這一輪只測、只記錄、只回報。

---

## 最重要的規則：分成兩個腦

這是這次測試的核心方法，請嚴格遵守，否則測不出東西。

### 🧑 第一個腦：「阿美」——第一次當助理的行政同仁

- 55 歲上下，會用 LINE、會用瀏覽器，**不懂程式、沒看過這個系統**
- 手上只有一句交代：**「下週三部門教育訓練，8 個人分兩組，玩 5 題，你負責操作。」**
- **在這個角色裡，嚴禁打開 `party/`、`client/`、`*.html` 的原始碼來搞懂怎麼操作。**
  只能看畫面、只能看畫面上的文字提示。
- 卡住的時候，先老實記下來：
  - 我在哪一步猶豫了？猶豫多久？
  - 我按錯了什麼？為什麼會按錯？
  - 我是用猜的嗎？猜對還是猜錯？
  - 我在找什麼東西但找不到？
- **只有在真的完全卡死時**，才可以切換到工程師視角去翻程式碼——
  但一旦這麼做，就要把這一項標成 **🔴 必須看程式碼才知道 = 最嚴重的卡點**。

### 🔧 第二個腦：「工程師」——事後才登場

跑完整場之後，才切回工程師視角，去定位每個卡點的成因與檔案位置。
**不要一邊操作一邊讀原始碼**，那會讓你「因為看過程式碼所以知道怎麼做」，
把真正的卡點蓋掉。（上一輪就是這樣，漏掉不少東西。）

---

## 開工前：先問 Vincent 兩件事

1. **`FIX-PLAN.md` 裡哪些項目已經修好了？**（修好的要在這輪重點驗收；沒修的先跳過別重複回報）
2. **測本機還是線上？**
   - 本機（建議）：不影響正式資料
   - 線上 `https://policy-gogogo.pages.dev`：更貼近實況，但會在 D1 留下事件紀錄

---

## 環境設定（上一輪踩過的坑，照這個做可以省一小時）

```
後端：npx wrangler dev -c worker/wrangler.jsonc --port 1998
前端：node .claude/static-server.mjs      （port 8123，serve public/）
```

一律用 `preview_start` 開伺服器，**不要用 Bash 跑 dev server**。

| 坑 | 對策 |
|---|---|
| 連接埠 **1999** 可能有先前 session 遺留、已無回應的 `workerd.exe` 殭屍行程 | 先 `netstat -ano \| findstr :1999` 確認；有的話直接改用 1998，或請 Vincent 手動關掉（你沒權限） |
| 本機沒有 Cloudflare Pages 的 `_redirects`，**`/pj` `/gamer` `/ass` 短網址不存在** | 直接開 `presenter.html` / `participant.html` / `assistant.html`，並帶 `?room=xxxx&host=127.0.0.1:1998` |
| **每支「手機」要有獨立的 `localStorage`**（deviceId），否則同一個 origin 的第二個分頁會把第一個踢掉 | 每支手機用不同 origin：不同 port（起多個靜態伺服器）或 `127.0.0.2` / `127.0.0.3` … 等 loopback 別名 |
| `preview_start` **最多同時 5 個伺服器** | 規劃好：後端 1 + 靜態 1 + 手機 3 |
| 瀏覽器面板若沒顯示，**所有分頁的 `document.visibilityState` 永遠是 `hidden`**，會被瀏覽器重度節流，背景分頁的 WebSocket 會斷掉且不自動重連 | ⚠️ **這是測試環境限制，不是產品 bug，不要當成 bug 回報。** 對策：讀取分頁狀態前先 `location.reload()`；或用 Node WebSocket bot 撐人數（連線穩定） |
| 截圖可能失效（面板沒顯示） | 用 `read_page`（`filter: "interactive"` 比較省）＋ `javascript_tool` 讀 `document.querySelector('.stage.active').innerText` |
| 助理端的功能分散在分頁裡 | 「設定」＝模式/題數/分組；「計分表」＝搶答與給分控制（開賽前是鎖住的）；「分組表」＝進退場紀錄 |

**人數配置建議**：真人 UI 操作 3 支手機（這是找卡點的主力），其餘 5 人用 Node WebSocket bot 撐場面。
bot 只要能 `player_join` 和 `buzz_press` 就夠——**這一輪不需要做任何偽造封包的測試**。

---

## 劇本

照著跑，不要自由發揮。每一步都要記錄「阿美」的感受。

### 幕 1 — 開場前 15 分鐘（籌備）

阿美第一次打開助理端。**先什麼都別做，看 10 秒。**

1. ⏱ **盲測**：打開助理端後，**10 秒內你知道下一步該做什麼嗎？** 記下你的第一直覺。
2. 她想把房號改成好記的數字（例如當天日期）。做得到嗎？在哪裡？
3. 她要把投影端接上投影機。**畫面有告訴她怎麼做嗎？** 照著做一次。
4. 她要產生給大家掃的 QR Code。找得到嗎？QR 會不會自己消失？消失了她知道怎麼叫回來嗎？
5. 她要設定：5 題、每題 5 分、分 2 組、普通模式。**這四件事分別在哪裡設？她找得到嗎？**

> 特別記錄：她有沒有在「房間 / 設定 / 分組表 / 計分表 / 題目 / 題庫」之間迷路？
> 有沒有哪個分頁她點進去完全看不懂是幹嘛的？

### 幕 2 — 大家陸續進場（8 人）

用不同 origin 讓 8 個人進場，名字用真實一點的中文姓名。過程中夾雜這些**一般人真的會做的事**：

- 有人**沒打名字就直接按「進入遊戲」**
- 有人**手抖連按兩下**「進入遊戲」
- 有人名字**打錯了想改**（他找得到怎麼改嗎？）
- 有人名字後面**多打一個空格**
- 有兩個人**名字一模一樣**（例如兩個「陳淑芬」）——阿美分得出誰是誰嗎？
- 有人**先進來又跑去廁所**（關掉分頁），5 分鐘後回來
- 有人**開了兩個分頁**（以為第一個沒反應）
- 組長想**幫小組取名字**——他找得到嗎？取名成功了嗎？

> 每一項都要記錄：**參賽者知道自己成功了嗎？畫面有告訴他嗎？**
> 以及：**阿美在助理端看得出來現在幾個人、誰在誰不在嗎？**

### 幕 3 — 場次一：普通模式 5 題

阿美要主持。每一題的循環是：開始搶答 → 有人搶到 → 他們口頭說要哪個分類 →
阿美點分類 → 確定 → 大家想答案 → 阿美聽完口頭回答 → 公佈答案 → 給分 → 下一題。

**每一題都要記錄阿美的困惑點**，特別是：

- 第 1 題：她知道要先按哪一顆嗎？「開始搶答」在哪？
- 讓**所有人都不要按**，看 8 秒後會發生什麼。**阿美看得出來是「沒人按」還是「真的有人按」嗎？**
- 讓一個長輩**在 321 數完之前就先按**。他的手機說什麼？他真的搶到了嗎？他知道嗎？
- 讓**沒搶到的那組猛按搶答鈕**。他們得到任何回饋嗎？
- 搶到的那組**答不出來**，阿美要怎麼處理？她找得到「重新搶答」嗎？她分得清楚「重新搶答／跳題／同範圍重抽」的差別嗎？
- 答案**只答對一半**，阿美要給幾分？「25% / 50% / 100% / 不計分」她按得下去嗎？她知道各是幾分嗎？
- 有一題**阿美覺得太難想換掉**——她會按哪一顆？結果符合預期嗎？
- 題目倒數**太長／太短**，她會怎麼調？調得動嗎？
- **答題中有人手機閃退**（關掉分頁再打開）——他回來看到什麼？阿美知道他掉線過嗎？
- **中途來了一個從沒進來過的人**——他進得來嗎？進哪一組？其他人有被動到嗎？

第 5 題結束後：阿美要**結束本場**。她找得到嗎？結束後投影幕、手機分別顯示什麼？
參賽者拿得到回顧檔嗎？**回顧檔的題數對不對？**

### 幕 4 — 中場：換一個助理接手（關鍵！）

模擬「阿美臨時要離開，換小林接手」：

1. 把助理端頁面**關掉再重開**（或直接 F5）
2. **小林沒有阿美的任何口頭交接**，他要接著開第二場
3. 記錄：他一打開看到什麼？設定還在嗎？他知道現在是什麼狀態嗎？
4. 他要開新的一場——**他知道要先按「重新開始」嗎？** 還是會直接去按「開始遊戲」？
5. 按下去之後**組名還在嗎？人還在嗎？**（這是 `FIX-PLAN.md` P1-1 的驗收）

> 另外測一次：**第二位助理用助理控制碼從另一台裝置登入**（右下角「投影或助理登入」）。
> 兩個助理同時開著，一個按按鈕，另一個看得到嗎？會不會打架？

### 幕 5 — 場次二：地獄模式 5 題 + 中途換搶答方式

1. 設定地獄模式、5 題
2. 開賽後，**中途把搶答方式從「電光石火」切成「狂點奪魁」**
   - 參賽者手機上顯示的規則正確嗎？
   - 叫一支手機重新整理，它顯示的規則還正確嗎？
3. 狂點奪魁那幾題：讓 3 支真人手機**盡全力猛點 5 秒**，看點擊數大概落在什麼區間
   （記下數字，這是之後判斷「多少算異常」的基準）
4. 玩到一半，**阿美/小林不小心按到「結束本場」**——有沒有機會反悔？
5. 玩到一半，**投影端那台電腦不小心被重新整理**——畫面回得來嗎？要不要重新輸入控制碼？
6. 打完 5 題，正常結束

### 幕 6 — 收尾

- 賽後回顧檔：下載一份、複製文字一份，內容對不對？題數對不對？
- 參賽者手機的結算頁：名次、分數、組員名單對不對？
- 阿美要**把成績留下來**給主管看，她有什麼辦法？夠不夠用？

---

## 全程要持續盯的三件事

1. **有沒有「按了沒反應」的按鈕？** 這是長輩最容易恐慌的地方。每發現一個就記下來。
2. **有沒有「畫面說成功、其實沒成功」的情況？** 這比按了沒反應更糟。
3. **助理端跟投影端跟手機端，三邊顯示的東西一致嗎？** 任何不一致都記下來。

---

## 產出格式

### A. 卡點表（主產出，按嚴重度排序）

| # | 幕 | 阿美想做什麼 | 她預期會怎樣 | 實際怎樣 | 卡點等級 | 建議 |
|---|---|---|---|---|---|---|

**卡點等級**（照這個分）：

| 等級 | 定義 |
|---|---|
| 🔴 5 | **必須看原始碼／問 Vincent 才知道**——新手完全做不到 |
| 🟠 4 | 試錯超過 3 次才成功，或做出錯誤結果而不自知 |
| 🟡 3 | 猶豫超過 10 秒、要來回切分頁找 |
| 🔵 2 | 找得到但位置不直覺 |
| ⚪ 1 | 小瑕疵，不影響完成任務 |

### B. 「按了沒反應」與「假成功」清單

分開列，每筆註明：哪個畫面、哪顆按鈕、什麼情況下、使用者會怎麼誤解。

### C. 三端不一致清單

每筆註明：助理端顯示什麼／投影端顯示什麼／手機端顯示什麼／哪個是對的。

### D. 具體文案修改建議

不要只說「文案要更清楚」，要**直接寫出建議的句子**。例如：

> `participant.html` 搶答鈕 locked 狀態
> 現在：`搶答`
> 建議：`本輪由 ○○組 作答`

### E. 「新手助理最需要的三樣東西」

跑完之後回答：如果只能為新手助理加三個東西（一段引導、一個提示、一顆按鈕都算），
你會加哪三個？為什麼？

### F. 環境限制聲明

老實列出這一輪**因為測試環境而測不到／測不準**的部分，不要混進卡點表。
（例：分頁 `visibilityState` 永遠 hidden 造成的斷線，不算產品問題。）

---

## 不要做的事

- ❌ 不要修改任何程式碼
- ❌ 不要測偽造 WebSocket 封包這類需要開 DevTools 的作弊（參賽者是一般長輩，不會做這個）
- ❌ 不要在「阿美」角色裡讀原始碼來搞懂怎麼操作
- ❌ 不要把測試環境造成的斷線當成產品 bug
- ❌ 不要只回報「有 bug」，要回報「**新手會在哪裡卡住、卡多久、然後他會怎麼辦**」

---

## 最後

用繁體中文回報。回報要能直接回答這一句：

> **「這套系統現在交給一個沒碰過的人，他辦得起來嗎？如果不行，還差什麼？」**
