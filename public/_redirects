# 短網址(200 = rewrite,網址列保留 /pj /gamer /ass;實體檔名不變)
/pj      /presenter.html    200
/gamer   /participant.html  200
/ass     /assistant.html    200

# 資管端(後臺報表)不需要在這裡 rewrite:因為 public/admin.html 這個「實體
# 檔」存在,Cloudflare Pages 的 clean-URL 會自動把 /admin 對應到 admin.html
# (並把 /admin.html 308 導到 /admin)。若在這裡再加 /admin -> /admin.html,
# 反而會跟 clean-URL 打架變成 /admin -> /admin 無限迴圈(踩過的坑)。
# /api/* 則由 functions/ 動態處理,本來就不經過 _redirects。

# 參賽者加入短連結(帶房號)→ /gamer?room=房號。兩種寫法:/r/房號(QR 用、
# 最短)與 /gamer/房號(語意化、好記)。由 functions/r/[code].ts 與
# functions/gamer/[code].ts 處理,「不能」寫在這裡,兩個坑(2026-08-20):
#
# 坑一(現場事故):_redirects 的 :placeholder 只代換目的地的「路徑」段,
# 放進 query string 不代換 —— 舊規則 `/r/:code /gamer?room=:code` 會把
# 所有人 302 到字面值 "/gamer?room=:code",全部進到同一個叫 ":code" 的
# 幽靈房,跟助理端的真房間永遠對不上(控制碼一直錯、名單看不到人)。
# 查詢字串的代換只能交給 Pages Functions。
#
# 坑二(修復當下):「新的」Functions 路由模式(例如第一次出現的
# /r/:code)部署後要 ~30-45 分鐘才在所有邊緣節點生效,期間同一網址
# 時而 302 時而 404;/api/* 老路由不受影響是因為模式早已生效,不是
# 前綴特權(在 /api/ 下新加路由一樣要重新暖機)。既有模式的重新部署
# 不會重暖。結論:動到 functions/ 的路由「結構」要提早部署,並用
# curl -sI 連打確認收斂,不要賽前才上。
