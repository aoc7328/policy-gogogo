# Dependencies
node_modules/

# PartyKit / Wrangler
.partykit/
.wrangler/

# Build output
dist/
build/

# Local env
*.local
.env
.env.local
.env.*.local

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code local settings (project-shared settings.json is OK to commit)
.claude/settings.local.json

# (quiz-bank-metadata.json used to be marked personal, but was promoted to
#  source of truth for branding + topic frameworks in the metadata-driven
#  refactor — now bundled into the PartyKit server build, so it must be tracked.)

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
