# 修正總表（2026-07-23 兩場實測後）

> ## ✅ 全部已實作（2026-07-23）
>
> Phase 1~4 已全數修完並驗證，**尚未經 Vincent 現場驗收**。
> 待驗收清單與驗收步驟見各項的「驗收方式」。
>
> **⚠ 後端要手動部署**：這次動到 `party/`，Vincent 必須雙擊一次 `sync-deploy.bat`，
> 否則線上只會有前端的改動，`party/` 的修正（含 P1-1 阻斷級）不會生效。
>
> **新增的驗證指令**
> ```sh
> npm run verify:all      # 純靜態,不需 server(已含新的 verify:buzzux)
> npm run verify:live     # 需先跑 npx wrangler dev -c worker/wrangler.jsonc --port 1998
> ```
>
> **刻意沒做的**：P4-3 的「投影端要求控制碼」。理由寫在該項底下。

> **這份文件原本的用途**：貼給新的對話串當工作清單。
>
> 資料來源：2026-07-23 用本機 wrangler dev 實際跑完兩場（普通模式 5 題、地獄模式 5 題 + 狂點奪魁），
> 8 位參賽者、2 組，含中途離線重連、中途進場、助理端重整等情境。

---

## 0. 動工前必讀（踩過的坑）

| 事項 | 說明 |
|---|---|
| **前端部署檔在 `public/`** | 根目錄的 `assistant.html` / `presenter.html` / `participant.html` 是舊快照，**不要改那些**。 |
| **`public/lib/partybus.js` 是建置產物** | 真正的原始碼是 `client/partybus.ts`。改完要跑 `npm run build:client`，產物要一起 commit。 |
| **動到 `party/` 要手動部署** | `party/*.ts` 是後端 DurableObject，改完必須由 Vincent 雙擊 `sync-deploy.bat`，否則線上不會生效。前端改動 push 到 master 即自動部署。 |
| **驗證腳本** | 每次改完至少跑 `npm run verify:all`。已知既有 baseline 誤報（不是 regression）：`verify:dom-refs` 2 筆、`audit:check` 的 wrangler/miniflare/undici/sharp 4 筆。 |
| **連接埠 1999** | 曾有先前 session 遺留、已無回應的 `workerd.exe` 殭屍行程佔住 1999。開工前先確認，必要時請 Vincent 手動關掉，或用 `--port 1998` 繞開。 |

---

## Phase 1 — 阻斷級：不修就不能上場

### P1-1　開新一場時全場從分組名單被清空，誰都搶答不了 🔴

**症狀**
參賽者改過組名 → 助理端重整過頁面 → 按「重新開始」→ 按「確定、開始遊戲」之後，
伺服器的分組變成：

```json
[{"name":"第一組","members":[],"leader":null},
 {"name":"第二組","members":[],"leader":null}]
```

8 個人全部從名單消失。參賽者手機還顯示舊組名（勇腳團／不老松），按搶答完全無效，
因為 server 用組名找不到那一組就直接丟棄封包。投影幕最後顯示：

> 第一組 · **(無人)** 搶答耗時 8.000 秒

**根因**
`party/state.ts` 的 `startGame()`：

```ts
const prevMembers = new Map(state.groups.map(g => [g.name, [...g.members]]));
state.groups = config.groups.map((g, i) => ({
  ...,
  members: prevMembers.get(g.name) ?? [],   // ← 用「組名」當 key
}));
// 後面的補救迴圈同樣用 g.name === p.team 比對
```

助理端重整後本機組名欄位是空的，送出的 `config.groups` 是預設「第一組／第二組」，
跟 server 上的「勇腳團／不老松」對不上 → 拿到空陣列；補救迴圈也對不上。

**建議修法（擇一或併用）**
1. `startGame()` 改用 `idx` 對應既有 members / leader，不要用組名。
2. 更根本：server 已經有權威的 `state.groups`，**開賽時不該讓 client 覆寫組名**。
   `config.groups` 只用來決定「幾組」，名字一律沿用 server 現有的。
3. 加防呆：若套用後 `members` 總數 < `state.participants.size`，記 log 並用 participants 重建。

**驗收方式（Vincent 測）**
1. 開房 → 2 人以上進場 → 讓組長把兩組都改名（例如「勇腳團」「不老松」）
2. **把助理端頁面重新整理一次**
3. 按「重新開始」→ 重新選模式與題數 → 按「確定、開始遊戲」
4. ✅ 助理端「計分表」的兩組名稱仍是勇腳團／不老松，人數正確
5. ✅ 按「開始搶答」，參賽者按得動，投影幕顯示的是真人名字而不是「(無人)」

---

### P1-2　助理端斷線時，畫面完全沒有提示 🔴

**症狀**
助理端 `PartyBus` 狀態已經是 `disconnected`，但助理人在「設定／房間／分組表／題目／題庫」
任一分頁時，**看不到任何斷線警示**，會繼續照常操作。

**根因**
警示元素 `.ctrl-status`（文字：「⚠ 與伺服器斷線 · 嘗試重新連線中」）被放在 `.ctrl-zone` 裡，
而 `.ctrl-zone` 屬於「計分表」那個 `.page`。切到別的分頁時該 page 是 `display:none`，
警示連同整個區塊一起被藏起來。

**建議修法**
把連線狀態指示搬到**永遠可見的頂部 bar**（跟房號／ROUND 同一排），或用 fixed 定位的橫幅。
狀態至少三種：連線中（綠）／重連中（黃）／斷線（紅，且要醒目到不可能忽略）。

**驗收方式**
1. 助理端開著，切到「設定」分頁
2. 把電腦 Wi-Fi 關掉（或關掉本機 server）
3. ✅ 不管在哪個分頁，畫面上方都立刻出現紅色斷線橫幅
4. Wi-Fi 開回來 → ✅ 橫幅自動消失

---

### P1-3　斷線期間助理按的鈕會靜默消失 🔴

**症狀**
在 `disconnected` 狀態下按「重新開始」：助理端畫面照常回到設定頁、狀態列還寫
「遊戲已重置」，但**伺服器完全沒收到**（phase 仍是 `ended`、分數還在）。
助理毫不知情，接著在錯誤狀態上疊操作 → 直接引爆 P1-1。

**根因**
`client/partybus.ts` 的 `emit()` 直接 `socket.send()`，沒有離線佇列、沒有重送、
送不出去也不通知使用者。

**建議修法**
1. `emit()` 先檢查 `socket.readyState !== OPEN`：
   - 特權指令（`PRIVILEGED_COMMAND_TYPES` 那些）→ **推進待送佇列**，重連成功後依序補送；
     或至少 **回傳 false 並讓呼叫端把該操作視為失敗**。
   - 高頻無狀態指令（`ping`、`buzz_press`）→ 直接丟棄即可，不入佇列。
2. 送不出去時觸發一個全域 callback，讓助理端跳出明確提示：
   「⚠ 指令沒有送出去（連線中斷），請等連線恢復後再按一次」。
3. `_doRestartImpl()` 等關鍵流程，改成「送出成功才更新本機 UI」，
   或等 server 的 `game_restart` 廣播回來才切畫面。

> 改完記得 `npm run build:client`。

**驗收方式**
1. 助理端開著、遊戲進行中
2. 關掉 Wi-Fi → 按「下一題」「重新開始」
3. ✅ 跳出明確提示說指令沒送出，畫面**不會**假裝已經執行
4. Wi-Fi 開回來 → ✅ 要嘛自動補送、要嘛提示「請重按一次」，狀態不會跟 server 打架

---

## Phase 2 — 場上看得到的錯誤顯示

### P2-1　「同範圍重抽 / 跳題」丟掉的題目仍算進總題數與賽後回顧

**症狀**：場次 1 實際只出了 5 題，投影端結算寫「共 **6** 題」，
參賽者回顧檔列出 Q01~Q06，其中 `Q04 · H-ES-003` 是被重抽掉、**沒有人答過**的題目，
而且把真正的第 4 題擠到 Q05。

**方向**：`askedQuestions` 在重抽／跳題時應該把被丟棄的那筆**移除或標記為 discarded**，
結算與回顧只計入實際作答過的題目。

**驗收**：開一場 5 題，中間按一次「同範圍重抽」→ ✅ 結算顯示「共 5 題」，回顧檔只有 Q01~Q05，
且沒有任何一題是沒人答過的。

---

### P2-2　投影端待機畫面的組數寫死 04組

**症狀**：實際只有 2 組，投影幕永遠顯示「04組 / Teams」。

**位置**：`public/presenter.html:937`
```html
<div class="stat-item"><div class="stat-value">04<span class="unit">組</span></div><div class="stat-label">Teams</div></div>
```
沒有 `id`，也沒有任何程式碼會更新它（隔壁的 `stat-remaining`、`stat-round` 都有 id）。

**方向**：加 `id="stat-teams"`，在 `game_start` / `roster_reshuffled` / `__room_state__` 時更新。

**驗收**：開 2 組 → ✅ 投影端顯示「02組」；改成 3 組 → ✅ 顯示「03組」。

---

### P2-3　投影端待機畫面顯示上一場的模式

**症狀**：場次 2 明明是地獄模式，投影端 STANDBY 畫面仍寫「ROUND 01/ 普通模式」「MODE · 普通」。

**方向**：待機畫面的模式標籤要吃 `game_start` 的 `mode`，不要留著上一場的值。

**驗收**：第一場普通、結束後開第二場地獄 → ✅ 投影端待機畫面寫「地獄」。

---

### P2-4　中途進場／重連的人看到錯的搶答規則

**症狀**：助理已經把搶答模式切到「狂點奪魁」，但重連或新進場的手機顯示
「電光石火 · 321 GO 後最快按下的組獲勝」。他會用錯的方式玩。

**根因**：`__room_state__` 裡有兩個欄位——`game.rushMode` 是**開場當下的設定值**，
頂層 `rushMode` / `rushModeActual` 才是**當前值**。參賽者端讀了前者。

**方向**：參賽者端（與投影端）還原搶答模式一律讀頂層 `rushMode`（或 `rushModeActual`）。

**驗收**：開賽用電光石火 → 助理切成狂點奪魁 → 叫一支手機重新整理 →
✅ 手機顯示「狂點奪魁 · 5 秒內猛點」。

---

### P2-5　重連的人看到的組員名單會多出已經離開的人

**症狀**：同一時間——
- 一直在線的手機：第二組 **4 人**（正確）
- 中途重連的手機：第二組 **5 人**（多一個早就離開的），底下寫「8 位夥伴已就位」但實際 7 人在線

**根因**：server 的 `g.members[]` 是刻意黏著的（`removeParticipantByConn` 註明不刪，
用來記住暫時斷線的人好讓他回原組——**這個設計本身是對的，不要拿掉**）。
問題在 `snapshot()` 把整份黏著清單送給新連線，而一直在線的端是靠 `player_leave`
事件把人刪掉的 → 兩邊永遠對不起來。

**方向（擇一）**
1. `snapshot()` 的 `members` 額外附上線上狀態，例如 `members: [{name, online}]`，
   三端統一顯示「在線 4 人（另有 1 人斷線中）」。
2. 或者反過來：一直在線的端收到 `player_leave` 時**不要**把人從名單移除，只標成灰色／離線，
   讓兩邊都顯示同一份黏著名單。
3. 人數統計一律以「在線」為準，不要用 members.length。

> 這一項是「顯示一致性」，不要為了修它去動 24 小時 deviceId 綁組的邏輯（那個運作正常）。

**驗收**：A、B、C 三人在場 → C 關掉手機 → ✅ A 看到 2 人 → D 這時進場並重新整理 →
✅ D 看到的人數與 A 一致（不會多出 C，或兩邊都標示「C 斷線中」）。

---

### P2-6　重新分組後組名不跟著人走

**症狀**：王秀琴把自己那組取名「勇腳團」，助理按「重新分組（全員隨機重洗）」後，
她被分到「不老松」，「勇腳團」變成別人那組。組名綁在**槽位**、不綁在**人**。

**✅ 已採用 (a)（Vincent 2026-07-23 決定）**：重新分組時把自訂組名一併清空回
「第一組／第二組…」，請各組組長重取。

- server：`setupTeams(state, count, resetNames)` 加第三個參數；`onTeamCountChanged`
  在真的要重洗時帶 `true`，並補一發 `score_update`，讓以組名為鍵渲染的畫面跟上。
- 助理端：確認框文字改成說明新行為（會清空組名、請大家重取）。
- 回歸測試：`npm run verify:regroup`。**已確認在修正前會紅**——舊版重洗後
  「勇腳團」留在原位、取名的王秀琴自己被丟到「不老松」。

**驗收**：兩組以上進場 → 組長各自取名 → 助理按「重新分組」→
✅ 三端組名都變回「第一組／第二組」，且沒有人從名單消失。

---

## Phase 3 — 55 歲以上使用者的公平性與順暢度

> 這一組是「交給任何人當助理都會用」的核心。P3-1 我認為對這個族群最不公平。

### P3-1　~~321 倒數期間先按 = 白按，而且畫面騙他~~ → **❌ 我判讀錯誤，已還原**

> **這一項作廢。原始報告是錯的，錯在我身上。**

**我當時寫的**：倒數期間按下去，手機會顯示「已搶答 · 等待判定」，但伺服器丟棄，
害人以為搶到了。

**實際上**：改動前的程式碼是 —— 倒數期間按鈕維持 `locked`，`handleBuzz()` 第一行
`if (!btn || btn.classList.contains('locked')) return;` **完全靜默、什麼都不做**，
`armBuzz()` 要等 GO 那 800ms 過完才呼叫（原始碼註解明寫「顯示 "GO" 800ms，
期間 buzz 仍鎖死」）。**它從來沒有顯示過「已搶答」。**

我第一次實測時看到「已搶答 · 等待判定」，是因為跨分頁的工具呼叫往返超過 3.8 秒、
GO 早就過了才按到 —— **我把自己的測試誤差當成了產品 bug。**

**Vincent 2026-07-23 決定：改回最初始狀態（完全靜默）。**

> 「所有的搶答規則都是『3、2、1、Go』才開始，我們就統一就好了。既然錯誤已經
> 發生了，而且今天大家已經玩過一次、已經理解了這個模式，如果這時候又改，
> 我覺得會讓大家的學習成本不停地提高。」

已還原：`handleBuzz()` 在倒數期間（`Date.now() < G.rushArmAt`）直接 `return`。
`rejectBuzz()` 的倒數分支已移除，它現在只處理「已經開搶、但這個人不能按」的情況
（＝ P3-3，那一項保留）。回歸測試 `verify:buzzux` 的 1c 已改成驗證「倒數期間完全靜默」。

**⚠ 已知但刻意不動**：`ARM_COUNTDOWN_MS = 3800`（3000 倒數 + 800 GO 停留）。
Vincent 原本要的是純 3 秒，「GO」是先前的 AI 自己加的，等於數了 4 秒。
但玩家已經學會這個節奏，**不要為了「修正」它而再改一次**。

**⚠ 給未來接手的人**：跨分頁 / 跨工具呼叫的往返延遲動輒 3～10 秒，
**絕對不能用它來測「幾百毫秒內」的行為**。要測時間窗請用 jsdom（`verify-buzz-ux.mjs`）
或在頁面內掛 `PartyBus.on('start_rush', …)` 自行排程。

---

### P3-2　沒人按時系統會隨機送一組，而且顯示得像有人按

**症狀**：`rush/speed.ts` 的 fallback——8 秒沒人按就隨機挑一組、挑該組第一個人，
投影幕顯示「**陳大明 搶答耗時 8.000 秒**」。助理完全看不出來其實全場沒人按。
（狂點模式下會顯示「(無人按)」，比較誠實。）

**建議修法**
- 投影端與助理端顯示改成「⏱ 時間到 · 無人搶答，由系統指定 **○○組** 作答」，不要編一個時間出來。
- 助理端加一顆「這輪不算，重新搶答」的明顯出口。

**驗收**：開始搶答後所有人都不要按 → ✅ 投影幕明說「無人搶答，系統指定」。

---

### P3-3　沒搶到的組按搶答鈕完全沒回饋

**症狀**：按鈕還是寫「搶答」、外觀是 `locked` 但 `disabled=false`，按下去毫無反應。
長輩會一直按、會覺得手機壞了。

**方向**：`locked` 狀態下按下去給一次輕提示（toast 或按鈕抖動）＋文案改成
「本輪由○○組作答」／「等主持人開始搶答」。

**驗收**：非搶答階段猛按 ✅ 有回饋文字。

---

### P3-4　助理端一重整，所有設定全部歸零

**症狀**：模式、總題數、每題配分、各題型秒數、進退場紀錄全清空，要整套重設。
分組資料在 server 有保住，但**這正是 P1-1 的觸發前提**。

**方向**：把助理端的本機設定（模式／題數／配分／秒數／組數／新手導覽開關）
存進 `localStorage`，重整後自動回填；進退場紀錄改由 server 快照補送。

**驗收**：設定好一整套 → F5 → ✅ 全部還在，不用重設。

---

### P3-5　狀態文字殘留

- 助理端狀態列常停在「▶ 搶答中」，其實已經進到選題／答題階段
- 參賽者端公佈答案後，畫面下方仍寫「答題中」

**方向**：狀態標籤統一綁 server 的 `phase`，不要各處自己維護。

---

### P3-6　空白名字時「進入遊戲」是灰的，但沒說為什麼

**方向**：按鈕下方常駐一行淡字「請先輸入你的名字」，或維持 disabled 但點下去給提示。

---

### P3-7　~~普通模式會抽到 3 分鐘的申論題~~ → **不是問題，這是設計**

**❌ 這一項作廢（Vincent 2026-07-23）。** 我原本把「申論題 180 秒」當成缺點，是誤判。

> 「現場不會空三分鐘，大家會瘋狂討論，這就是設計這個題目、設計這個題型、
> 設計這個秒數的用意。這個遊戲最大的目的不是得分，而是透過這個遊戲去思考，
> 怎麼樣做好一個保險業務。」

**3 分鐘是刻意留給小組討論的時間，不是空等。** 已把先前擅自改成 120 秒的
三處全部還原為 **180**（`i-timer-essay` 的 value、`S.timerDefaults`、`TIMER_DEFAULTS`）。

**⚠ 給未來接手的人**：這個系統的設計目標是「引導思考」，不是「比分數」。
看到「效率不佳／等待時間長／不夠刺激」之類的觀察時，先確認那是不是刻意的設計，
再決定要不要動它。

---

## Phase 4 — 防呆與作弊（優先度低，對象是一般人）

> Vincent 說參賽者只是一般人、頂多有點小聰明，不會偽造封包。
> 這一組**可以最後做，或直接不做**，但先記著。
> 其中 **P4-1 是唯一一般人也碰得到的**（連點器、多指亂點、找人幫忙一起點）。

| # | 問題 | 一般人碰得到嗎 | 方向 |
|---|---|---|---|
| **P4-1** | 狂點奪魁沒有任何頻率上限。實測一個人 5 秒送出 **7,440 次**，人均 1860 次；同時另一組真人猛點只有 48 次 | ✅ **會**（連點器 App、多指狂拍、旁人幫按） | server 對每個連線每秒的 `buzz_press` 設上限（例如 15/秒），超過的丟棄不計數 |
| P4-2 | `onBuzzPress` 直接信任封包裡的 `name` / `team`，可以冒充別組別人 | ❌ 需要開 DevTools | server 改用**連線本身**的身分（`sender.state`）決定組別與名字，不看 payload |
| P4-3 | 投影端不需控制碼：知道房號就能開 `presenter.html?room=xxxx`，答案文字在 DOM 裡（只是 `opacity:0`） | ⚠️ 有點小聰明就做得到（房號印在投影幕上） | 投影端連線要求 `controlCode`；答案未公佈前不要 render 進 DOM |
| P4-4 | `rename_self` 沒有重名檢查也沒有階段限制，可以改成別人的名字 | ⚠️ 打字就做得到 | 重名時拒絕並提示；遊戲開始後鎖定改名 |

> 補充：`buzz_press` 的時間戳 server 已經覆蓋成收到時間，偽造時間戳無效 —— 這點原本就做對了。

---

## 附錄：這次實測確認「沒有問題」的部分（不要動壞它）

- **分組穩定性**（Vincent 最在意的）——全部實測通過：
  - 有人閃退後重開手機回來 → 回原組，其他人零異動
  - 他離線期間來了從沒進來過的新人 → 新人只補進人數最少的組
  - 助理端重整頁面 → 分組完全不變（舊版「助理一重連全場重洗」確實修好了）
  - 按「重新開始」開新一場 → 同名的人回原組
  - 有人惡意把名字改成別組某人的名字 → 本人靠 **deviceId 24 小時鎖**仍留原組
  - 唯一會動到既有組員的只有「重新分組（全員隨機重洗）」，且有二次確認
- 房號鎖：投影端就緒後輸入框／套用／重新生成全部 disabled
- 控制碼可以打小寫
- 多開分頁：舊分頁會蓋上「⚠ 本分頁已停用」遮罩
- 連按兩下「進入遊戲」不會重複加入；5/5 之後再按「下一題」不會爆
- 「重新分組」「重新開始」「結束本場」都有二次確認
- 重連還原很完整：題目、選項、剩餘秒數、分數、失格狀態都回得來
- 「重新搶答」的失格提示很清楚（「本題你們已搶答過 · 這輪不參與」）

## 附錄：這次沒測到的

- `/report` 賽後報告頁（讀線上 D1，本機沒資料）
- `/admin` 資管端（同上）
- 線上正式環境的連線穩定度（本機 wrangler dev 在約 10 條 WebSocket 下會自己 churn）
- 真實手機瀏覽器（Safari iOS / Android Chrome）的行為
